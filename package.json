{
  "name": "nexo-gestao-financeira",
  "private": true,
  "scripts": {
    "heroku-postbuild": "npm --prefix frontend ci && npm --prefix frontend run build && rm -rf frontend/node_modules"
  },
  "engines": {
    "node": "24.x",
    "npm": "11.x"
  }
}
